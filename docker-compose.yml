version: '3.9'

services:
  pricing-api:
    build: .
    ports:
      - "8000:8000"

  streamlit:
    build: .
    command: streamlit run app/dashboard/streamlit_app.py --server.port 8501 --server.address 0.0.0.0
    ports:
      - "8501:8501"